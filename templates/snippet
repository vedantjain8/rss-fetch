    <!-- title -->
    <h1>{{ post.title }}</h1>

    <!-- summary -->
    <p>{{ post.summary }}</p>

    <!-- if else for author and published date -->
    {% if " " not in post.date_posted and " " not in post.author %}
    <p>By {{ post.author }} on {{ post.date_posted }}</p>

    {% elif " " not in post.date_posted %}
    <p>On {{ post.date_posted }}</p>

    {% elif " " not in post.author %}
    <p>By {{ post.author }}</p>

    {% else %}
    <p></p>

    {% endif %}

    <!-- full href -->
    <a href="{{ post.link }}" target="_blank" rel="noreferrer noopener">
  <div class="container">
    
    <!-- image -->
    {% if " " not in post.image %}
    <img
      src="{{ post.image }}"
      alt="{{ post.title }} Image"
      class="img-thumbnail"
      class="img-responsive"
    />
    {% else %} {% endif %}

<!-- full code -->
{% extends "layout.html" %} {% block HtmlbodyContent %} {% for post in posts %}

{{ post.site }}
{{ post.title }}
{{ post.summary }}
        
{% if " " not in post.date_posted and " " not in post.author %}
<p>By {{ post.author }} on {{ post.date_posted }}</p>

{% elif " " not in post.date_posted %}
<p>On {{ post.date_posted }}</p>

{% elif " " not in post.author %}
<p>By {{ post.author }}</p>

{% else %}
<p></p>

{% endif %}

{% endfor %} 

{% endblock HtmlbodyContent%}

<!-- part 2 -->
<div class="card">
  <div class="card-header">{{ post.site }}</div>
  <div class="card-body">
    <h5 class="card-title">{{ post.title }}</h5>
    <p class="card-text">{{ post.summary }}</p>
    <a
      href="{{ post.link }}"
      target="_blank"
      rel="noreferrer noopener"
      class="btn btn-primary"
      >Visit</a
    >
  </div>
  <div class="card-footer text-muted">
    {% if " " not in post.date_posted and " " not in post.author %}
    <p>By {{ post.author }} on {{ post.date_posted }}</p>

    {% elif " " not in post.date_posted %}
    <p>On {{ post.date_posted }}</p>

    {% elif " " not in post.author %}
    <p>By {{ post.author }}</p>

    {% else %}
    <p></p>

    {% endif %}
  </div>
</div>
{% endfor %} {% endblock HtmlbodyContent%}